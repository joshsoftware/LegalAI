"""
api/ — FastAPI layer for the translation service.

  models.py  — Pydantic request/response schemas
  routes.py  — all endpoints (health, translate/text, translate/files)

The FastAPI app is created in api_server.py at the project root.
"""
